<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Forzar barra final si no es un archivo real
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_URI} !(.*)\/$
  RewriteRule ^(.*)$ $1/ [L,R=301]

  # Error 404
  ErrorDocument 404 /404.html
</IfModule>
